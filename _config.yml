exclude: [assets/less, docker]

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "post"
      author: "Mark Siebert"

domain: "https://msiebert.github.io/"

gems:
  - jekyll-sitemap
